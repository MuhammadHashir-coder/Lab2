//Implement an algorithm for determining if an Nth is a divisor of an n Number (i.e. 2 is a divisor of 6).
If so, determine if it’s an even number or odd number as well.

1. Ask the uset to enter a number "n"
2. Ask to give a number to divide "nth"
3. Set divisibility to (n%nth)
4. IF divisibility=0 
      Display "nth is divisor"
   else 
      Display "Not divisible"
5. SET eo=n%2
6.IF eo=0  //eo is even and odd
     Display "even"
  else 
     Display "odd"


//Implement an algorithm where the user enters a number, and an appropriate month is displayed.
1. 
2. INPUT number from 1-12
3. IF number==1
      Display "January"
4.  IF number==2
      Display "Febuary"
5.  IF number==3
      Display "March"
6.  IF number==4
      Display "April"
7.  IF number==5
      Display "May"
8.  IF number==6
      Display "June"
9.  IF number==7
      Display "July"
8.  IF number==8
      Display "August"
9.  IF number==9
      Display "September"
10.  IF number==10
      Display "October"
11.  IF number==11
      Display "November"
12.  IF number==12
      Display "December"
13. else
      Display "Incorrect number"
14. END
